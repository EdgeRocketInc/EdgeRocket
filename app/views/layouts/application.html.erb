<!DOCTYPE html>
<html ng-app="EdgeRocket">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "EdgeRocket" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "EdgeRocket" %>">
    <%= stylesheet_link_tag "application", media: "all" %>
    <%= stylesheet_link_tag "yeti", media: "all" %>
    <%= javascript_include_tag "application", controller_name if controller_name != 'sessions' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main">
       <%= render 'layouts/messages' %>
       <%= yield %>
    </main>
  </body>
</html>
